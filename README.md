# Simple tool to copy things to the clipboard
- cd [solution directory]
- update app.config with your presets [optional]
- dotnet build -c=Release
- update your config file with your presets [optional].
